{
  "name": "nx-vitest-monorepo",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": "23.x"
  },
  "workspaces": [
    "ui/packages/*"
  ],
  "scripts": {
    "test": "nx run-many --target=test --all --skip-nx-cache"
  },
  "devDependencies": {
    "@types/node": "^22.13.14",
    "@vitest/browser": "3.1.1",
    "nx": "20.4.4",
    "playwright": "^1.51.1",
    "vitest": "3.1.1"
  }
}
